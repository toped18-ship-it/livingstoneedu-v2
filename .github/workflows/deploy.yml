name: Deploy to InfinityFree

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy Website
    runs-on: ubuntu-latest
    
    steps:
    - name: Get Latest Code
      uses: actions/checkout@v4

    - name: Sync Files via FTP
      uses: QuadraSystems/FTP-Deploy-Action@1.1.0
      with:
        ftp-server: ftpupload.net
        ftp-username: ${{ secrets.FTP_if0_42169733}}
        ftp-password: ${{ secrets.FTP_yJBfoEeiZ9fj3}}
        local-dir: ./
        server-dir: /htdocs/livingstoneedu.great-site.net
        
        
